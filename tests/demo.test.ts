import {describe, test} from '@jest/globals'



describe('Module Name', () => {
    test('this is a test', () => {
        //expect(function).toBe(resultExpected)
        if( false ){
            throw new Error('no puede dividir entre cero')
        }
        
    })
})